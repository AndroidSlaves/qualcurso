Class QualCurso:  extends Application 

*************************************************

VARIABLES
	
*instance: It's a static variable of the type QualCurso. Used to instanciated the database.

*databaseName: It's a static variable of the type String. Used to named the database.

***************************************************

FUNCTIONS
	
*QualCurso(): Constructor that receive the name of the database and the istance.
		
*setDatabaseName(String databaseName): Void function. Used to access databaseName space of memory store database there.
	
*getDatabaseName(): String function. Used to access the databaseName values and return it.

*static QualCurso getInstance(): Statict function typed QualCurso. Used to access the "instance" values and return it.

